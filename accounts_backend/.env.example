# PostgreSQL connection (choose either DATABASE_URL or individual PG* variables)
DATABASE_URL=

PGHOST=localhost
PGPORT=5432
PGDATABASE=accounts_db
PGUSER=postgres
PGPASSWORD=

# IMPORTANT RLS NOTE:
# The backend must set the tenant on each DB connection/session prior to queries:
#   SELECT set_config('app.current_tenant', '<tenant-uuid>', true);
# With Knex:
#   await knex.raw(\"SELECT set_config('app.current_tenant', ?, true)\", [tenantUuid]);
